# Verified env (2026-05-17) on RTX PRO 6000 Blackwell, CUDA 13.0, Python 3.12.

# Core — transformers must be 4.x. 5.x removes ROPE_INIT_FUNCTIONS['default']
# and changes DynamicCache API in ways the Fast-dLLM v2 custom modeling.py
# does not handle. Fast-dLLM v2 trust_remote_code is pinned to 4.x conventions.
torch==2.10.0
transformers==4.57.6
peft>=0.19.1
trl>=1.4.0
accelerate>=1.1.1
datasets

# Fast-dLLM v2 LoRA injection requires torchao>=0.17.
torchao>=0.17.0

# Eval
sacrebleu>=2.4

# Misc
sentencepiece
matplotlib
pandas
bitsandbytes>=0.44.1
